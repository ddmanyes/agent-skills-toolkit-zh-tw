---
name: sp-code-review
description: 審查指定 diff、工作樹或實作與規格的差異，以 Standards／Spec 雙軸回報具體問題；適用一般程式碼及 Python／生物資訊改動。
allowed-tools: Terminal, Read, Write, Edit, Glob, Grep, Task
---
# 進階程式碼審查

保留兩條獨立的判準：Standards 檢查專案規範與 code smell；Spec 檢查需求是否落實。各自報告證據與嚴重度，避免一軸通過掩蓋另一軸問題。

## 定義範圍

1. 先採用使用者指定的檔案、提交、分支或未提交修改；查看 Git 狀態與有效 ref。
2. 比對兩個指定版本用兩端 diff；分支新增工作用 merge-base 到 HEAD；工作樹審查包含 staged、unstaged 及相關 untracked 檔案。記錄實際範圍和指令，不能用固定的三點 diff 取代所有模式。
3. 未指定時，從當前改動、PR 或任務上下文推導並說明。只有不同基準會改變審查結果且無法判斷時才問。空 diff 回報無改動；無效 ref 先修正，不能當作審查通過。

## 兩軸審查

- **Standards**：讀適用的 AGENTS.md、實際專案指示、CONTEXT.md／風格指南及變更相關設定；以 [SMELLS.md](SMELLS.md) 作判斷基線。專案規範優先；smell 是需證據支持的判斷，不能只因命名相似就判錯。
- **Spec**：優先使用使用者需求與已定案對話，再找 issue、PRD、規格檔。指出漏做、做錯及未要求的行為；引用需求與對應程式位置。無可核對規格時明確標為未評估，繼續可完成的 Standards 審查。
- 大範圍且環境允許時可讓兩個子代理獨立審查，傳入範圍及必要規範／來源路徑；無共享檔案工具時才附必要原文。小 diff 可在同一代理內分兩軸檢查。

## 驗證與完成

根據改動影響選擇既有測試、lint、build 或可重現案例；先查已存在的可靠結果。純審查不用為開始而強迫整套測試全過。Python／Scanpy 專案依既有 uv 環境執行。區分原有失敗、新增失敗、工具缺失與未執行項目。

以繁體中文分別列出 Standards 與 Spec findings：位置、觸發條件、實際影響、依據、修正建議。已確認問題與待實測疑慮分開；沒有問題也要說明覆蓋範圍。內容長度隨證據需要，不以固定字數截掉重要問題。完成條件是已檢查的範圍與每項發現可追溯，並列明剩餘盲點。
