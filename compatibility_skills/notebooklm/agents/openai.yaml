interface:
  display_name: "notebooklm"
  short_description: "Personal workflow compatibility"
  default_prompt: "Use $notebooklm for the requested workflow."
policy:
  allow_implicit_invocation: true
