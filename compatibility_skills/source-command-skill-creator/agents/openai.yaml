interface:
  display_name: "source-command-skill-creator"
  short_description: "Legacy command compatibility"
  default_prompt: "Use $source-command-skill-creator for the requested workflow."
policy:
  allow_implicit_invocation: false
