interface:
  display_name: "source-command-pptx-skill"
  short_description: "Legacy command compatibility"
  default_prompt: "Use $source-command-pptx-skill for the requested workflow."
policy:
  allow_implicit_invocation: false
