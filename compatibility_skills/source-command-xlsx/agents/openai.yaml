interface:
  display_name: "source-command-xlsx"
  short_description: "Legacy command compatibility"
  default_prompt: "Use $source-command-xlsx for the requested workflow."
policy:
  allow_implicit_invocation: false
