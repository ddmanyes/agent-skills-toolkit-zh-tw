interface:
  display_name: "session-snapshot"
  short_description: "Personal workflow compatibility"
  default_prompt: "Use $session-snapshot for the requested workflow."
policy:
  allow_implicit_invocation: true
