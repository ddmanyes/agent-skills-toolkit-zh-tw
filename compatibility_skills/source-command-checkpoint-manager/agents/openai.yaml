interface:
  display_name: "source-command-checkpoint-manager"
  short_description: "Legacy command compatibility"
  default_prompt: "Use $source-command-checkpoint-manager for the requested workflow."
policy:
  allow_implicit_invocation: false
