interface:
  display_name: "source-command-ralph-loop"
  short_description: "Legacy command compatibility"
  default_prompt: "Use $source-command-ralph-loop for the requested workflow."
policy:
  allow_implicit_invocation: false
